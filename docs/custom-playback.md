# 自定义页面的播放模式

时间轴下方的文字按钮在“原片播放”和“回合播放”之间切换；英文分别为 Source playback 和 Rally playback。新草稿默认原片播放，同一草稿内切换页面、取消导出后保留所选模式。返回模式选择、打开新草稿或重启后恢复默认。

原片播放沿原视频时间轴播放。回合播放依次播放视频轨上已勾选片段的当前起止范围，包括手动新增和调整后的片段，跳过间隔；最后一段结束时暂停，再次主动播放才从第一段重新开始。

- 播放中切入回合模式，在片段内部继续，在间隔中跳到下一段；已在所有片段之后则原地暂停。切回原片模式从当前位置继续。暂停时切换不移动播放头、不启动播放。
- 拖动播放头时可以自由查看任意位置。暂停时松开保留位置；播放时松开到空隙则进入下一段。暂停后再次播放也按当前位置进入回合。
- 点击列表中的未勾选回合会临时播放该回合，不改变选择或导出内容。结束后衔接时间上的下一段已选回合；若已进入某个已选片段则直接继续，没有后续片段则暂停在临时片段末尾。暂停后可以继续临时预览；点击别的回合、手动定位或切换模式会替换或结束临时预览。
- 勾选、取消、增加、删除和调整边界实时生效。暂停时不自动跳转；播放中当前范围失效则进入下一段。
- 视频轨完全为空时，两种模式都按原片播放，此规则优先于临时回合预览。所选模式仍保留，重新加入片段后按所选模式播放。

这些模式仅控制监视窗口预览，不改变分析结果、片段选择、历史记录或导出格式。跳转继续使用现有兼容预览链路，加载和代理准备期间以最后一次播放或暂停意图为准。

## 滚轮缩放工具

播放模式按钮前的放大镜按钮开启“缩放视频轨”。开启后，在时间轴标尺或片段轨道上向上滚动放大、向下滚动缩小，以鼠标位置为缩放中心；再次点击按钮或在自定义工作区右键可取消，恢复普通滚轮横向滚动。原有 Ctrl/⌘＋滚轮缩放入口继续可用。

缩放工具与增加、删除工具互斥，不改变播放模式。开启时仍可定位播放头、点击片段播放和调整片段边界，缩放只作用于时间轴。

## 验证记录（2026-09-12）

- 类型检查及差异空白检查通过。自定义播放决策、播放控制、自定义页面、时间轴、片段、兼容预览及应用流程相关测试共 113 项通过；新增与修改后只重跑对应受影响项。
- `node scripts/verify-custom-playback.mjs` 的 16 项检查通过，使用真实乒乓球视频、实际自定义页面和 `ttcut-media` 协议，检查跳转后的时间位置及解码帧计数增加。
- 覆盖实时切换、自然跨回合、最后一段暂停及主动重播、暂停空隙后播放、未选回合临时预览、空轨回退、重新勾选、快速切换、中英文按钮与键盘操作。最后一段设定结束时间为 28 秒，实测暂停位置为 28 秒。
- 测试在本机 macOS / Electron 中运行，使用独立用户目录和软件解码。测试桥接的平台字段为 `win32`，原生系统及解码器仍是 macOS，不能作为 Windows 解码或安装器验证。
- 缩放工具补充验证：自定义页面及时间轴相关测试 22 项、类型检查通过；`node scripts/verify-custom-playback.mjs --zoom-only` 的 4 项真实 Electron 检查通过，覆盖普通滚轮缩放、右键取消、快捷键与键盘操作、工具互斥及中英文按钮布局，并确认监视窗口和页面缩放比例不受影响。

未执行 Windows 实机验证、安装器构建或远端发布。
