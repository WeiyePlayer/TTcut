# TTcut Windows v1.3.6

**简体中文** | [English](https://github.com/WeiyePlayer/TTcut/blob/v1.3.6/docs/release-notes-v1.3.6.en.md)

`v1.3.6` 是面向 Windows x64 的正式版本更新。本次将分析模型、运行时和视频工具完整放入安装包，同时补齐自定义时间线编辑能力并修复分析边界问题。

## Windows 分析运行时

- 完整安装包内置 BlurBall 与球桌分析 ONNX 模型、Python 3.12、ONNX Runtime DirectML/CPU，以及固定的 x264 FFmpeg/FFprobe；安装后不再下载或导入模型与媒体组件。
- 支持时优先使用 DirectML；若完整分析阶段运行失败，会丢弃该次未完成输出并从第 0 帧重新使用 CPU 分析，避免混合两套不完整结果。
- 完整分析保持逐帧处理，不包含 `temporal_stride` 或插值帧实现。两个冻结真实视频的 CPU/DirectML 对照分别得到完全一致的 56 个回合/259 个落点和 16 个回合/69 个落点；该结果只证明这两个样本，不推导为所有设备与视频均等价。

## 自定义剪辑与时间线

- 恢复“多选”菜单，可全选或按 1–10 的最低板数批量选择回合；旧连续运动记录缺少板数元数据时会明确提示重新分析。
- 相邻片段共用边界时，根据拖动方向选择左侧末尾或右侧开头；播放头命中区域不再遮挡片段轨道。
- 新增独立的当前编辑回合：非当前片段显示为浅蓝色，播放头靠近边界 8 像素内可吸附，`A`/`D` 可调整当前片段左右边界，并支持新增片段模式。

## 分析与视频修复

- 自动球桌标定在 OpenCV 跳过目标时间点时，会从更早帧重试采样。
- 混合回合结果在严格校验前规范化重叠区间并重新计算板数；FFprobe 报告的 HEVC 时长略短时，保留已经验证的 Worker 时间线边界。
- x264 兼容输出改用非 JPEG 的有限范围像素格式，避免颜色范围错误。
- 组件完整性提示现在明确区分校验失败与正常的内置状态。

## 安装包

- **Windows x64 · v1.3.6**：`TTcut-1.3.6-x64-Setup.exe`，只提供包含模型、运行时和视频工具的完整安装包。
- 安装包使用固定 `CN=weiye` 自签名 Authenticode 证书、RFC 3161 时间戳和签名更新清单。未信任该证书的 Windows 环境仍可能显示“未知发布者”或 SmartScreen 提示。
- 本次 GitHub Release 保持草稿状态，暂不公开发布，也不创建 `v1.3.6` 远端标签。

## 验证

- TypeScript 类型检查通过；Vitest 共 62 个测试文件、439 项测试通过，另有 6 个文件、24 项测试按既有条件跳过；Python Worker 共 198 项回归测试通过。
- 官网生产构建及 2 项渲染测试通过。
- Windows x64 打包应用验收 10/10 通过，覆盖完整逐帧 Worker、板数多选、相邻边界、播放头命中、Windows 混合结果和旧历史兼容；界面数据使用确定性记录，不作为真实视频落点准确率证据。
- 正式安装包通过 ONNX 模型、最小 Worker、DirectML/CPU 运行时和仅 x264 视频包边界校验；应用、卸载程序与外层安装包均带固定证书和 RFC 3161 时间戳，签名更新清单、SBOM 与 SHA-256 摘要已生成。
